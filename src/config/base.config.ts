export class Config {
    static windowWidth: number = window.innerWidth;
    static width: number = Config.windowWidth * 0.75;
    static height: number = 400
    static player = {
        health: 10
    }
}